Encender servicios de xampplite y entrar a phpMyAdmin

Exportar la base de datos Recomendacion.sql en phpMyAdmin

Instalar desde Microsoft Store la herremeinta Python

En la terminal ir a la direccion del proyecto ingresar los siguientes comandos:

  python -m pip install fastapi mysql-connector-python pandas requests
  
  python -m uvicorn backend.api:app --reload
  
Abrir el archivo login.html
